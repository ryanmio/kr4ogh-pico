# Supabase sink credentials. Copy to .env (gitignored) and fill in.
# The tool only writes; the service role key must never ship to a client.
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY
