Archived flights land here, one directory per flight (`flight.mdx` +
`track.json`), written by the flight-archive export when a flight closes.
The site renders whatever is in here statically; empty is a valid state.
